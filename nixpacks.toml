providers = ["python", "node"]

[variables]
NODE_ENV = "development"

[phases.install]
cmds = ["cd web && npm ci --include=dev", "pip install -r requirements.txt"]

[phases.build]
cmds = ["cd web && npm run build"]

[start]
cmd = "python -m uvicorn src.api:app --host 0.0.0.0 --port ${PORT:-8000}"
